# Development + CI dependencies. Runtime deps live in requirements.txt.
-r requirements.txt

pytest==9.1.1            # test runner
pytest-cov==7.1.0        # coverage measurement + the CI coverage gate
respx==0.23.1            # mock httpx calls, so tests never touch the Groq API
ruff==0.16.5              # linter + formatter
mypy==2.3.1             # static type checking
pip-audit==2.10.1         # dependency vulnerability scanning in CI
pre-commit==4.6.2        # run the above on every commit
