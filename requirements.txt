# MeetSaransh — deliberately minimal. Every line is strictly required.
fastapi==0.115.6         # web framework + request/response validation
uvicorn==0.34.0          # ASGI server to run the app
httpx==0.28.1            # HTTP client for the Groq ASR + LLM calls
python-multipart==0.0.20 # required by FastAPI to accept audio file uploads
python-dotenv==1.0.1     # load the Groq API key from a local .env file

# Storage (SQLite) and JSON handling use the Python standard library on purpose,
# to keep the dependency footprint small (see README "Design decisions").
